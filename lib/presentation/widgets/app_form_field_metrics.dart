import 'package:flutter/material.dart';

class AppFormFieldMetrics {
  static const double denseHeight = 48;
  static const double denseIconSlot = 42;
  static const double denseIconSize = 20;
  static const EdgeInsets denseContentPadding = EdgeInsets.symmetric(
    horizontal: 12,
    vertical: 10,
  );
  static const EdgeInsets denseDropdownContentPadding = EdgeInsets.symmetric(
    horizontal: 0,
    vertical: 10,
  );

  AppFormFieldMetrics._();
}
